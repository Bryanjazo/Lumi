# Lumi · Redundancy & Simplification Audit (Jul 10)

12 findings, soul explicitly keep-listed. Owner decisions marked ★.

## Big structural calls — ★ owner decides
1. ★ MERGE Focus tab into Home (L) — two full entry points to the
   identical session; Home's card is 1 tap vs tab+3 steps. Frees a
   nav slot (5 tabs → 4). Keep the hearth session screen + duration
   presets; cut the duplicate picker wizard.
2. ★ Onboarding 10 → 7 screens (S-M) — cut the widget step (collects
   nothing pre-value), defer companion-mode to Profile (defaults
   'full' anyway), make the reflection reveal instant.
3. ★ Vocabulary unify (S, copy-only) — "Edit quest" vs "N things" vs
   "task"; tier names Trial/Task/Whim mean a low task is "a Task
   that isn't a Task" and "Trial" collides with the free trial.
   Proposal: "task" everywhere; tiers → Big/Regular/Tiny.
4. ★ CUT Profile's stat grid (M) — Me's bond narrative and Profile's
   2×2 grid show the same numbers one tap apart. Stats live once
   (Me, the narrative version).
5. ★ MERGE "What Lumi knows" (M) — full section in Profile + row on
   Me + /insights screen = three surfaces. One canonical screen,
   doors elsewhere. Hide the learning meter's fake 20% floor.
6. ★ MERGE backlog nudge into the waiting card (M) — two cards
   describing the same open pile two cards apart.

## Quick wins — no real decision, do on your word
7. Hero meta diet (S): the "one thing" card carries tier+window+kind
   +xp (~8 signals before the title). Keep window; tier/XP surface
   at completion.
8. Notification smart defaults (S): quiet hours always-on from the
   sleep anchor (info, not toggle); meds toggle renders only when
   meds context exists. 5 toggles → 2.
9. One recurrence path (S): accepting a suggestion applies Lumi's
   detected schedule in one tap; sheet only via "adjust".
10. MoveBackToDateSheet fast path (S): Today/Tomorrow buttons above
    the full picker.
11. Shared load vocabulary (S): Untangle's pile header says "light
    day · 3 things" using Time's load words.

## Keep-listed (do NOT simplify away)
12. The layered double-undo (toast + history pill) — the good kind of
    redundancy for this audience; XP-farm guard makes it safe.
